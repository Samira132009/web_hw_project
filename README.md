# Blog Platform

Полнофункциональная блог-платформа с бэкендом на Spring Boot и фронтендом на React.

## 🚀 Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

#### macOS/Linux:
```bash
# Сначала установите зависимости
npm install

# Затем запустите
npm run dev
```

Или используйте скрипт:
```bash
chmod +x start.sh
./start.sh
```

Если возникают проблемы с concurrently, используйте простой скрипт:
```bash
chmod +x start-simple.sh
./start-simple.sh
```

#### Windows:
```bash
start.bat
```

### Вариант 2: Через npm (требует установки зависимостей)

```bash
# Установить все зависимости
npm run install:all

# Запустить оба сервера одновременно
npm run dev
```

### Вариант 3: Ручной запуск

#### Терминал 1 - Бэкенд:
```bash
./gradlew bootRun
```

#### Терминал 2 - Фронтенд:
```bash
cd frontend
npm install
npm run dev
```

## 📋 Требования

- **Java 17+** - для бэкенда
- **Node.js 18+** - для фронтенда
- **npm** или **yarn** - менеджер пакетов

## 🏗️ Структура проекта

```
.
├── src/                    # Бэкенд (Spring Boot)
│   ├── main/java/         # Java код
│   └── main/resources/    # Конфигурация
├── frontend/              # Фронтенд (React + TypeScript)
│   ├── src/              # React компоненты
│   └── package.json      # Зависимости фронтенда
├── build.gradle.kts      # Конфигурация Gradle
├── package.json          # Скрипты для запуска
└── start.sh / start.bat  # Скрипты запуска
```

## 🔧 Доступные команды

### В корне проекта:

- `npm run dev` - запустить бэкенд и фронтенд одновременно
- `npm run dev:backend` - запустить только бэкенд
- `npm run dev:frontend` - запустить только фронтенд
- `npm run build` - собрать оба проекта
- `npm run install:all` - установить все зависимости
- `npm run test` - запустить все тесты

### Бэкенд (Gradle):

- `./gradlew bootRun` - запустить приложение
- `./gradlew build` - собрать проект
- `./gradlew test` - запустить тесты

### Фронтенд:

- `cd frontend && npm run dev` - запустить dev сервер
- `cd frontend && npm run build` - собрать для production
- `cd frontend && npm test` - запустить тесты

## 🌐 Порты

- **Бэкенд**: http://localhost:8080
- **Фронтенд**: http://localhost:3000
- **API**: http://localhost:8080/api

## 📝 API Документация

После запуска бэкенда доступна Swagger UI:
- http://localhost:8080/swagger-ui.html

## 🧪 Тестирование

### Бэкенд тесты:
```bash
./gradlew test
```

### Фронтенд тесты:
```bash
cd frontend
npm test
```

### E2E тесты:
```bash
cd frontend
npm run test:e2e
```

## 🐳 Docker (опционально)

Для запуска через Docker:

```bash
docker-compose up
```

## ⚙️ Конфигурация

### Бэкенд
Конфигурация в `src/main/resources/application.yml`

### Фронтенд
Конфигурация в `frontend/vite.config.ts`

## 🔐 Авторизация

JWT токен автоматически сохраняется после входа и добавляется во все запросы.

## 📚 Дополнительная документация

- [Документация фронтенда](./frontend/README.md)
- [API документация](http://localhost:8080/swagger-ui.html)

## 🐛 Решение проблем

### Порт уже занят
Если порт 8080 или 3000 занят, измените их в конфигурационных файлах.

### Ошибки при запуске
1. Убедитесь, что установлены все зависимости: `npm run install:all`
2. Проверьте версии Java (17+) и Node.js (18+)
3. Очистите кэш: `./gradlew clean` и `cd frontend && rm -rf node_modules`

## 📄 Лицензия

MIT

